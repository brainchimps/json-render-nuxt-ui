{
  "name": "json-render-nuxt-ui-workspace",
  "private": true,
  "license": "MIT",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "demo": "pnpm --filter demo dev",
    "demo:build": "pnpm --filter demo build",
    "demo:test": "pnpm --filter demo test"
  }
}
